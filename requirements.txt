pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
openpyxl>=3.1   # Edgewonk .xlsx export
streamlit>=1.43,<1.59 # dashboard. floor 1.43 = width='stretch' API; ceiling
                      # <1.59 because newer minors changed query_params/URL
                      # behavior — broke the calendar "Back to month" button.
                      # 1.58.0 validated; session_state fallback also guards it.
plotly>=5.18    # interactive charts + candlestick trade-on-chart
anthropic>=0.40 # AI Coach (toai/ai_coach.py) — Claude API; pip installs latest
