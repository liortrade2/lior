# TOAI גרסה ניסיונית — TOAIExporterTick (תיקון ה-~2 שניות)

גרסה **נפרדת** של ה-Exporter שמרעננת את הציון / השער / הבאנר **בכל טיק**,
במקום רק בסגירת בר. נועדה לצמצם את הפיגור של ~בר אחד (2 דקות) שבגרסה
היציבה ל-~2 שניות.

## למה זה בטוח
- מחלקה חדשה לגמרי: `TOAIExporterTick`. **הגרסה היציבה `TOAIExporter` לא נגעה.**
- אם הקובץ לא מתקמפל — פשוט מוחקים אותו ומקמפלים מחדש, ו-`TOAIExporter`
  ממשיך לעבוד.
- הגרסה היציבה מגובה גם פיזית: `C:\LIOR_ML_archive\code_v1_working_20260616`
  וגם ב-git: commit `4c808f3`.

## איך משווים בין שתי הגרסאות (A/B)
שתי הגרסאות כותבות לאותם קבצים (`C:\LIOR_ML\<מכשיר>\`), אז מריצים **רק
אחת בכל פעם** לכל מכשיר:

**גרסה יציבה (v1):** ה-`TOAIExporter` שכבר על הגרף.

**גרסה ניסיונית (v2):**
1. ‏NinjaScript Editor → הדבק את `TOAIExporterTick.cs` → ‏F5 (קמפול).
2. בגרף: הסר את `TOAIExporter`, הוסף `TOAIExporterTick`.
3. ב-BloodHound (חוק ה-Threshold Rules): שנה את ה-solver מ-`TOAIExporter.MLPass`
   ל-`TOAIExporterTick.MLPass`.
4. **בדוק ב-Sim** (Sim101) — לא בחשבון אמיתי.

## לחזרה לגרסה היציבה
הסר את `TOAIExporterTick` מהגרף, החזר את `TOAIExporter`, והחזר את ה-solver
ב-BloodHound ל-`TOAIExporter.MLPass`. (אפשר להשאיר את שתי המחלקות מקומפלות
במקביל — רק לא להריץ את שתיהן יחד על אותו מכשיר.)

## ההבדל הטכני
| | v1 (TOAIExporter) | v2 (TOAIExporterTick) |
|---|---|---|
| Calculate | OnBarClose | OnEachTick |
| ייצוא features | בסגירת בר | בסגירת בר (זהה) |
| קריאת score.txt + שער + באנר | בסגירת בר בלבד | בכל טיק (throttled לפי זמן הקובץ) |
| פיגור השער | ~בר אחד (2 דק') | ~2 שניות אחרי סגירת בר |

**מגבלה שנשארת:** הציון של בר N קיים רק ~2ש' אחרי שהוא נסגר, אז סיגנל
**בדיוק** בסגירת בר עדיין נשען על ציון הבר הקודם. ה-v2 עוזר כשהכניסה
מתבצעת כמה שניות אחרי הסגירה — בדיוק מה שצריך למדוד ב-A/B.
